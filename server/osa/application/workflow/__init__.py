"""Application-layer workflow orchestrators (#160).

Orchestrators here span multiple domains (validation, curation, deposition,
record, feature, ingest) at stage boundaries; the application layer is their
DDD-correct home so domain-to-domain imports stay forbidden.
"""
